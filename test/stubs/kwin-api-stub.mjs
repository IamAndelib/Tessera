// Test stub for "kwin-api" (and its subpaths). Only symbols the engine
// actually uses at runtime need to exist here; everything else is type-only.
export const LayoutDirection = { Horizontal: 1, Vertical: 2 };